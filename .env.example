# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_AUTH=plain
SMTP_TLS=true
SMTP_DOMAIN=kiket.dev

# Email Settings
EMAIL_FROM=notifications@kiket.dev

# Rate Limiting
RATE_LIMIT_PER_MINUTE=20

# Preferences
ENABLE_SUPPRESSION=true

# Server
RACK_ENV=development
PORT=9393
